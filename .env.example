# No environment variables are required for the current static site.
#
# Do not commit real secrets. If server-side integrations are added later,
# document placeholder names here and keep actual values in local .env files
# or the deployment provider's secret manager.
